# Handoff — WEN-359: Maker agent definition (O2)

- Issue: https://linear.app/wenova/issue/WEN-359
- Branch / worktree: agent/WEN-359 (worktree agent-a104cf54df1143fb5)
- Commit(s): see branch head (`git log -1 agent/WEN-359`)
- Maker: claude (Opus 4.8 1M) · Date: 2026-07-14

## Scope / files changed
- `.claude/agents/maker.md` — new; reusable Claude Code subagent definition the orchestrator
  dispatches as `agentType:'maker'`. YAML frontmatter (`name: maker`, one-line `description`,
  `tools: Read, Write, Edit, Bash, Grep, Glob`) + a task-agnostic system-prompt body encoding
  the MAKER contract from `docs/agentic-operating-model.md` §6.

## Acceptance criteria — per-AC status
| AC | Status | Evidence (test / command / manual) |
|----|--------|------------------------------------|
| Prompt: read AGENTS.md + the issue (the spec); follow spec-package if present | PASS | §1 of `maker.md` body: reads AGENTS.md/CLAUDE.md, the Linear issue as spec, and an APPROVED `docs/spec-package/`. |
| Developability check → NOT_DEVELOPABLE with the blocking question, no invented behavior | PASS | §2: stop + return `NOT_DEVELOPABLE` with the exact blocking question; explicit "do NOT invent product behavior/enum/copy/schema/scope". |
| Implement only in scope; no unapproved libraries | PASS | §3: no files outside task scope, no libs/patterns beyond AGENTS.md; Zod-validate mutations, spec enums only, never commit secrets. |
| Run the full gate green | PASS | §4: `npm run typecheck && npm run lint && npm run test && npm run build`, capture exit codes, no handoff on red. |
| Write `docs/handoffs/<ISSUE-ID>.md` | PASS | §5: writes handoff using the exact template in `docs/handoffs/README.md`. |
| Commit + push branch `agent/<ISSUE-ID>` | PASS | §5: commit on `agent/<ISSUE-ID>` with Co-Authored-By line, then `git push -u origin agent/<ISSUE-ID>`. |
| Do NOT change Linear state or merge to main | PASS | §5: explicit — no merge to main, no Linear state/labels, no self-review. |
| Return structured `{status, perAC, gates, branch, sha, handoffPath, assumptions}` | PASS | §6: lists exactly these fields. |
| Frontmatter tools = broad maker set, nothing it must not use | PASS | `tools: Read, Write, Edit, Bash, Grep, Glob` — edit files, run git/npm, search; no Linear/merge-only tooling granted. |
| Body is English and task-agnostic (works for any task) | PASS | No feature-specific reference; all instructions parameterized by `<ISSUE-ID>`. |

## Gate results
- typecheck: exit 0 · lint: exit 0 · test: exit 0 (31 passed, 4 files) · build: exit 0

## Decisions / assumptions
- Tool set restricted to `Read, Write, Edit, Bash, Grep, Glob` per the task brief. Git and npm
  run through `Bash`; Linear state changes are intentionally NOT grantable to the maker (the
  orchestrator owns them), so no MCP tools are included.
- The maker uses git via `Bash` (branch/commit/push) rather than a dedicated git tool — matches
  the repo's environment (no separate git tool exists).

## Known risks / follow-ups
- none. This is a markdown agent definition; it adds no runtime code, so the gate is unaffected.

## How to verify
1. `cat .claude/agents/maker.md` — confirm frontmatter (`name: maker`, `description`,
   `tools: Read, Write, Edit, Bash, Grep, Glob`) and that the body encodes every §6 contract
   point (read spec → developability/NOT_DEVELOPABLE → in-scope implement → full gate → handoff →
   commit+push branch, no Linear state/merge → structured return).
2. Cross-check the body against `docs/agentic-operating-model.md` §6 and the O2 acceptance in
   `docs/agentic-operating-model-plan.md`.
3. `npm run typecheck && npm run lint && npm run test && npm run build` → all exit 0.
4. `git status` → clean after commit.
