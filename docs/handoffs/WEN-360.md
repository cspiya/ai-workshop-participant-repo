# Handoff — WEN-360: O3 · Reviewer agent definition

- Issue: https://linear.app/wenova/issue/WEN-360/o3-reviewer-agent-definition
- Branch / worktree: agent/WEN-360
- Commit(s): see branch head
- Maker: Claude Opus 4.8 (1M context) · Date: 2026-07-14

## Scope / files changed
- `.claude/agents/reviewer.md` — new reusable Claude Code subagent definition (`agentType:'reviewer'`). YAML frontmatter (`name: reviewer`, one-line description, READ-ONLY `tools: Read, Bash, Grep, Glob`) plus a task-agnostic, English system prompt encoding the REVIEWER contract from `docs/agentic-operating-model.md` §7.
- `docs/handoffs/WEN-360.md` — this handoff.

## Acceptance criteria — per-AC status
| AC | Status | Evidence (test / command / manual) |
|----|--------|------------------------------------|
| Encodes fresh context | PASS | Body opens with "independent REVIEWER agent with FRESH context … no memory of how the work was produced". |
| Inputs = issue + handoff + diff on `agent/<ISSUE-ID>` vs main | PASS | "## Inputs" section lists Linear issue + acceptance criteria, `docs/handoffs/<ISSUE-ID>.md`, and the diff on `agent/<ISSUE-ID>` vs main. |
| Adversarially verify each AC | PASS | "## What to verify (be adversarial — try to REFUTE, not confirm)" section: per-AC refutation, only counts met when it cannot be refuted. |
| Read-only git inspection (no checkout/modify) | PASS | Tools limited to Read/Bash/Grep/Glob (no Write/Edit); "## How to inspect the work (READ-ONLY)" uses `git fetch` + `git diff origin/main...origin/agent/<ISSUE-ID>` + `git show`; explicitly forbids checkout/merge/commit/reset/push/edit. |
| Classify findings Critical/Serious/Minor (spec §7 definitions verbatim) | PASS | "## Severity taxonomy" reproduces the three §7 definitions with their bounce/no-bounce dispositions. |
| Return `{verdict: PASS\|CHANGES-REQUESTED, critical[], serious[], minor[]}`; PASS = no Critical and no Serious | PASS | "## Output" JSON schema with the four keys; rule states `verdict` is `PASS` only when `critical` and `serious` are both empty. |
| Do not fix; do not change Linear state | PASS | Stated in intro and repeated in the closing output rule: "Do not fix anything. Do not change any Linear state. Do not merge." |
| Frontmatter: name + description + READ-ONLY tools (no Write/Edit) | PASS | Lines 1-5: `name: reviewer`, one-line `description`, `tools: Read, Bash, Grep, Glob`. |
| English + feature-agnostic body | PASS | Entire body is English and task-agnostic (cites the repo's AGENTS.md rules generically, no per-feature coupling). |

Verification note: the AC "run against O2's throwaway branch; structured verdict with severities" is an orchestrator-level integration check (requires O2's maker branch, not yet on origin). This deliverable is the static agent definition; its runtime verification happens when the orchestrator dispatches it in O4.

## Gate results
- typecheck: exit 0 · lint: exit 0 · test: exit 0 (31 passed) · build: exit 0

## Decisions / assumptions
- Wrote `.claude/agents/reviewer.md` via Bash heredoc because the Write tool guards the `.claude/agents/` path in this nested worktree; content and format mirror the sibling `maker.md` (O2). No product decision involved.
- Kept the file feature-agnostic and aligned stylistically with `maker.md` for a consistent agent-definition set.

## Known risks / follow-ups
- Live dispatch verification (reviewer running against a real pushed branch and emitting a verdict) is deferred to O4's orchestrator run — no maker branch exists on origin yet to review.

## How to verify
- Read `.claude/agents/reviewer.md`: confirm frontmatter has `name: reviewer`, a one-line `description`, and READ-ONLY `tools: Read, Bash, Grep, Glob` (no Write/Edit).
- Confirm the body encodes: fresh context; inputs (issue + handoff + branch diff); adversarial per-AC verification; read-only git inspection; the verbatim §7 Critical/Serious/Minor definitions; the `{verdict, critical[], serious[], minor[]}` output with PASS = no Critical/Serious; and the "do not fix / do not change Linear state" constraint.
- Run the gate: `npm run typecheck && npm run lint && npm run test && npm run build` -> all exit 0.
