# Handoff — WEN-358: Linear lifecycle setup + orchestrator config + handoff convention (O1)

- Issue: https://linear.app/wenova/issue/WEN-358
- Branch / worktree: agent/WEN-358 (worktree agent-a8c5e63c6aebf8427)
- Commit(s): see `git log -1` on branch (SHA recorded on push)
- Maker: claude (Opus 4.8 1M) · Date: 2026-07-14

## Scope / files changed
- `docs/handoffs/README.md` — new; explains the handoff convention + contains the exact §8 handoff template.
- `docs/orchestrator/config.md` — new; limits table (§5 defaults), gate command, target project scope, label-based lifecycle mapping (§3 option B).
- Linear (Wenova team): created 3 issue labels `agent:in-review`, `agent:changes-requested`, `agent:needs-human`.

## Acceptance criteria — per-AC status
| AC | Status | Evidence (test / command / manual) |
|----|--------|------------------------------------|
| Lifecycle sub-states exist in Linear (labels chosen, option B) + documented in config.md | PASS | 3 labels created via Linear MCP (ids `b54c5930…`, `d3dba9e4…`, `c2a8f4c2…`); mapping table in `docs/orchestrator/config.md`. |
| config.md records MAX_MAKERS/BOUNCE_LIMIT/AGENT_TIMEOUT/PER_RUN_TASK_CAP/RETRY + target project id | PASS | Limits table (§5 defaults) + Target scope table with project id `5b4c1325-ebce-470c-939d-18db3ee2274a` in `docs/orchestrator/config.md`. |
| docs/handoffs/README.md contains the exact handoff template | PASS | §8 fenced `markdown` template block copied verbatim into `docs/handoffs/README.md`. |
| Verification: git status clean after commit; labels visible in Linear | PASS | Gate green; commit made; labels returned by Linear MCP create calls. |

## Gate results
- typecheck: exit 0 · lint: exit 0 · test: exit 0 (31 passed, 4 files) · build: exit 0

## Decisions / assumptions
- Chose **option B (labels)** over dedicated states — the Wenova team workflow uses the default
  `Todo`/`In Progress`/`Done` states; labels avoid a workspace-workflow change and are writable
  via MCP. Documented mapping in config.md.
- Label colors chosen to echo the meaning (blue = in-review, amber = changes-requested,
  red = needs-human); not load-bearing.

## Known risks / follow-ups
- none. Labels are created but not yet applied to any issue — that is the orchestrator's job at
  runtime, not part of this setup task.

## How to verify
1. `cat docs/handoffs/README.md` — confirm the §8 template block is present verbatim.
2. `cat docs/orchestrator/config.md` — confirm limits table, gate command, project id
   `5b4c1325-ebce-470c-939d-18db3ee2274a`, and the label→lifecycle mapping.
3. In Linear (Wenova team) → Settings → Labels: confirm `agent:in-review`,
   `agent:changes-requested`, `agent:needs-human` exist.
4. `npm run typecheck && npm run lint && npm run test && npm run build` → all exit 0.
5. `git status` → clean after commit.
